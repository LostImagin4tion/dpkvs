apiVersion: v1
kind: ConfigMap

metadata:
  name: dpkvs-config
  namespace: dpkvs

data:
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "50051"
  FEATURE_CONFIG_PATH: "configs/features/feature_config.yaml"
  PERSISTENCE_LOG_PATH: "data/append-only-log.txt"
