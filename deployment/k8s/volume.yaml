apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: dpkvs-data
  namespace: dpkvs
  
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
  